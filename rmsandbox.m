function rmsandbox()
    if ~isempty( ver( 'fcam' ) )
        fx.fcam.command.enableaddon();
        fx.fcam.command.rmsandbox();
    else
        thisPath = fileparts( mfilename( 'fullpath' ) );
        rmpath( fullfile(...
            thisPath,...
            'code',...
            'fcam' ) );
        rmpath( fullfile(...
            thisPath,...
            'test' ) );
    end
end